const form=document.querySelector('#chat-form');if(form){form.addEventListener('submit',async(event)=>{event.preventDefault();const message=form.message.value.trim();if(!message)return;const chat=document.querySelector('#chat');chat.insertAdjacentHTML('beforeend',`<p><strong>You:</strong> ${message.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}</p>`);const body=new FormData(form);const response=await fetch(form.dataset.url,{method:'POST',body,headers:{'X-CSRFToken':form.querySelector('[name=csrfmiddlewaretoken]').value}});const data=await response.json();if(!response.ok){chat.insertAdjacentHTML('beforeend',`<p class="error">${data.error}</p>`);return}const patterns=data.reply.reported_patterns.length?`<p><strong>Reported patterns:</strong> ${data.reply.reported_patterns.join('; ')}</p>`:'';chat.insertAdjacentHTML('beforeend',`<article><p>${data.reply.reflection}</p>${patterns}<p><strong>Follow-up:</strong> ${data.reply.follow_up_question}</p></article>`);form.message.value='';});}
